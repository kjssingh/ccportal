lsi: false
safe: true
source: ccportal/
incremental: false
include: [header, bootstrap, css, img, js, kt, less, plugins, components, datatables, bootstrap-table, dist, jquery, datatables-plugins, plugins/*]
highlighter: rouge
gist:
  noscript: false
kramdown:
  math_engine: mathjax
  syntax_highlighter: rouge
